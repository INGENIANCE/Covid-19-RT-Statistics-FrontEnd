import { FilterIncPipe } from './filter-inc.pipe';

describe('FilterIncPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterConfirmedPipe();
    expect(pipe).toBeTruthy();
  });
});
