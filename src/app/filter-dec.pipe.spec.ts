import { FilterDecPipe } from './filter-dec.pipe';

describe('FilterDecPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDecPipe();
    expect(pipe).toBeTruthy();
  });
});
