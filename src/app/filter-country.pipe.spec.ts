import { FilterCountryPipe } from './filter-country.pipe';

describe('FilterCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
